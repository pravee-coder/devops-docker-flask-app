# Dockerized Flask App

A simple Python Flask application containerized using Docker.

## Tech Stack
- Python
- Flask
- Docker

## How to Run
```bash
docker build -t praveen-devops-app .
docker run -p 5000:5000 praveen-devops-app
